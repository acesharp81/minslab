---
name: CSS 규칙
globs: "**/*.css"
---

- `:root` CSS 변수 기반 디자인 시스템을 사용한다 (색상, 간격, 폰트 크기 등).
- 반응형은 모바일 우선(min-width 미디어쿼리)으로 작성한다.
- 다크 모드는 `prefers-color-scheme` 로 대응한다.
- 매직 넘버 대신 정의한 CSS 변수를 재사용한다.
